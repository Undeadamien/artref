services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.example
      - .env
    volumes:
      - .:/app
      - cache_data:/home/artref/.cache/artref
    command: uvicorn artref.api.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  cache_data:
