[project]
name = "artref"
version = "0.1.0"
description = ""
requires-python = ">=3.7"
dependencies = [
  "aiocache",
  "aiohttp",
  "diskcache",
  "typer",
]

[project.scripts]
artref = "artref.cli.main:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
