[project]
name = "artref"
version = "0.1.0"
description = ""
requires-python = ">=3.7"
dependencies = [
  "typer",
]

[project.scripts]
artref = "cli.main:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
